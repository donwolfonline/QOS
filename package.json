{
  "name": "qos-monorepo",
  "private": true,
  "packageManager": "pnpm@10.34.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "build:web": "turbo run build --filter=web",
    "dev": "turbo dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
