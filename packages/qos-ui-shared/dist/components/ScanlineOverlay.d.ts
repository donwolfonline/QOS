import React from 'react';
export interface ScanlineOverlayProps {
    opacity?: number;
    color?: string;
}
export declare const ScanlineOverlay: ({ opacity, color }: ScanlineOverlayProps) => React.JSX.Element;
